'use client';

/**
 * Placeholder destination for Notifications → Personal Intelligence Snapshot (PRD-001 §5.9).
 * Full snapshot UI ships in FTUE-09.
 */
export function SnapshotScreen() {
  return (
    <div
      className="min-h-screen flex items-center justify-center"
      style={{ background: '#070B14' }}
      aria-label="Personal Intelligence Snapshot"
    />
  );
}
