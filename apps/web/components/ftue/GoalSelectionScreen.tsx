'use client';

/**
 * Placeholder destination for Welcome → Goal Selection (PRD-001 §5.2).
 * Full multi-select goal UI ships in a later FTUE task.
 */
export function GoalSelectionScreen() {
  return (
    <div
      className="min-h-screen flex items-center justify-center"
      style={{ background: '#070B14' }}
      aria-label="Goal Selection"
    />
  );
}
