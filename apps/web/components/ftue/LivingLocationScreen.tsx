'use client';

/**
 * Placeholder destination for Birth Place → Living Location (PRD-001 §5.7).
 * Full living-location UI ships in a later FTUE task.
 */
export function LivingLocationScreen() {
  return (
    <div
      className="min-h-screen flex items-center justify-center"
      style={{ background: '#070B14' }}
      aria-label="Living Location"
    />
  );
}
