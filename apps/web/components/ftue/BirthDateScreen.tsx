'use client';

/**
 * Placeholder destination for Decision Profile → Birth Date (PRD-001 §5.4).
 * Full date-picker UI ships in a later FTUE task.
 */
export function BirthDateScreen() {
  return (
    <div
      className="min-h-screen flex items-center justify-center"
      style={{ background: '#070B14' }}
      aria-label="Birth Date"
    />
  );
}
