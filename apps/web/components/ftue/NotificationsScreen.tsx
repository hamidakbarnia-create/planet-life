'use client';

/**
 * Placeholder destination for Living Location → Notifications (PRD-001 §5.8).
 * Full notification preferences UI ships in a later FTUE task.
 */
export function NotificationsScreen() {
  return (
    <div
      className="min-h-screen flex items-center justify-center"
      style={{ background: '#070B14' }}
      aria-label="Notifications"
    />
  );
}
