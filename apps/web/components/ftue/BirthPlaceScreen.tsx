'use client';

/**
 * Placeholder destination for Birth Time → Birth Place (PRD-001 §5.6).
 * Full place autocomplete / map / GPS UI ships in a later FTUE task.
 */
export function BirthPlaceScreen() {
  return (
    <div
      className="min-h-screen flex items-center justify-center"
      style={{ background: '#070B14' }}
      aria-label="Birth Place"
    />
  );
}
