'use client';

/**
 * Placeholder destination for Birth Date → Birth Time (PRD-001 §5.5).
 * Full time accuracy / picker UI ships in a later FTUE task.
 */
export function BirthTimeScreen() {
  return (
    <div
      className="min-h-screen flex items-center justify-center"
      style={{ background: '#070B14' }}
      aria-label="Birth Time"
    />
  );
}
